git rev-list --all --objects | \
  awk -v git_dir="$(git rev-parse --git-dir)" \
    '/^[0-9a-f]{40} / {print $2}' | \
  xargs -I {} du -k -d 0 "$git_dir/objects/{}" | \
  sort -n

# to sort files in ascending order by size. Largest at bottom


git gc --prune=all
# git garbage collector : cleans up the Git history and remove references to non-existing files

# to get the sizes of the files
git ls-files -z | xargs -0 du -sh


# git lfs tracking 
git lfs track ziX8TAn5

# BFG Repo-Cleaner (to delete large files)
brew install bfg 

# create virutal env
python3 -m venv venv

# activate virtual env 
source venv/bin/activate


# creating lambda 
amplify create function
create virtual env*
pip install pipenv
pipenv install
pipenv shell



pip install -r requirements.txt


# to create lambda
make sure python version is the same in all 3 files and run the amplify shell

# remove and reinstall node modules 
cd /Users/yannellym/Desktop/ShelterLink
rm -rf node_modules package-lock.json
npm install
#Update react-scripts:
npm install react-scripts@latest
npm start


# update react scrips dependency
npm install react-scripts@latest


# for "amplify not connected"
- deleted the pip files and the env within the function
- activated the env in root
- navigated to func folder
- amplify push --force
amplify